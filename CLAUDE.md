# BatteryMonitor Claude Code Memory

@AGENTS.md

## Claude Code 补充约定

- 以上 `AGENTS.md` 是本项目规则的权威来源；不要在本文件复制一份并形成两套可能漂移的规则。
- 开始任务时先确认项目路径、Git 工作区和本次允许修改的范围。
- 结论先行，默认使用中文，避免空泛赞美；发现方案存在问题时直接说明证据和更稳妥的做法。
- 大改动前先在 Plan Mode 给出方案、影响范围和验证方法，等待用户确认。
- 如果项目现状与记忆文件不一致，以当前代码和用户最新指令为准，并先说明差异；需要更新项目规则时，先征得用户确认。
