# BatteryMonitor 发布前优化审查报告

> **历史快照，不是现行发布或 QA 操作手册。** 本文保留 2026-08-02 当次证据、旧路径和旧命令名，仅用于追溯；当前构建、测试、签名和运行规则只见 [`QA-RUNBOOK.md`](QA-RUNBOOK.md)，不得照本文的历史产物路径执行。

审查日期：2026-08-02

审查范围：语言完整性、macOS/CPU 架构兼容性、后台功耗、持久化与异常输入鲁棒性

版本：`1.2.0 (4)`（本次不改版本号，不上传 App Store Connect）

## 结论

本轮没有改变电量、续航、功率、温度和健康度的产品口径。优化集中在运行成本与失败边界：补齐代码实际使用的本地化键，移除动态系统字段中的固定中文，降低后台任务优先级与定时器刚性，批量保存续航历史，并为外部语言包、进程 CPU 数值和缓存写入增加保护。

最终 Release 构建、静态分析、逻辑测试、本地化测试、Apple Development 签名以及 arm64/x86_64 启动验证均通过。

## 优化前后对比

| 维度 | 优化前 | 优化后 | 结果 |
|---|---|---|---|
| 语言包结构 | 10 个包，每包 764 个键；代码扫描发现 1 个键缺失 | 10 个包，每包 790 个键；535 个代码字面键全部存在 | 不再把缺失键名直接显示给用户 |
| 动态系统数据 | 新字段分组、可靠性、异常原因固定为简体中文 | 26 个动态/辅助功能键提供 10 种语言原生文本 | 非中文界面不再在这些路径混入中文 |
| 格式符安全 | 运行时按英文包过滤，但发布脚本不做全量签名对比；循环说明中的字面 `%` 会被误识别 | 十种语言逐键校验 C 格式符签名；字面百分号统一转义 | 避免翻译包导致错误参数读取或静默回退英文 |
| 外部语言包 | 可读取任意大小、任意数量和长度的字符串 | 单包 2 MiB、5,000 项、键 200 字符、值 20,000 字符上限 | 损坏或恶意语言包不会无限占用内存 |
| 定时器 | 电池与进程均为严格 10 秒定时 | 两个定时器都允许 1.5 秒容差 | macOS 可合并唤醒，界面频率仍保持 10 秒 |
| 进程采样 | 后台 QoS 为 `userInitiated`；服务释放时未显式取消定时器 | QoS 改为 `utility`；`deinit` 主动取消定时器 | 降低调度抢占，避免遗留空定时器持续唤醒 |
| 续航历史写盘 | 每个有效的约 56 秒样本重写最多 10,000 条 JSON | 首次写入后最多每 5 分钟批量写入，暂停/退出强制冲刷 | 稳态约 64.3 次/小时降至 12 次/小时，理论减少 81.3% |
| 缓存完整性 | SOC 与充电历史为普通覆盖写 | 相关 JSON 均采用原子替换 | 中途退出时更不容易留下半截文件 |
| 异常 CPU 数据 | 负值、NaN、Infinity 或异常尖峰可能进入排序/图表 | 非有限值和负值归零，按逻辑核心数限制上界 | 睡眠唤醒、进程退出和计数器异常不再污染 UI |
| 强制操作 | 审查路径存在 `.first!`、`timer!`、正则 `try!` 三处 | 应用 Swift 源码中 `try!`/`.first!`/`timer!` 为 0 | 环境差异不再直接触发崩溃 |
| 隐私默认过滤 | 仅按简体中文分组名隐藏标识字段 | 同时按 serial/UUID/machine ID 路径与多语言身份分组过滤 | 默认视图和截图更不易暴露设备标识 |

## 语言完整性边界

结构完整性已经达到：十个语言包键集合完全一致，代码中能静态识别的本地化键零缺失，新增动态文案均有十种原生译文，并且发布脚本会阻止以后再次缺键或破坏格式符。

本轮没有用机器翻译批量覆盖历史高级说明，因为错误的技术翻译比明确回落英文更危险。现有包中仍有与英文完全相同的历史值，主要集中在完整技术说明和原型迁移文案：德语 101、日语 87、西班牙语 471、法语 476、意大利语 470、韩语 461、葡萄牙语 471、繁体中文 417；其中也包含 `CPU`、`macOS`、单位和字段名等本来就应保持不变的内容。它们不会显示原始键或混入固定简体中文，但不能据此宣称所有 790 条都经过母语人工审校。

## 兼容性验证

- 最低系统保持 macOS 14.0，Swift 5.9；没有引入更高系统版本 API。
- Release 二进制同时包含 `arm64` 与 `x86_64`。
- Release App 使用现有 `Apple Development` 证书签名并通过严格签名校验。
- Apple Silicon 原生启动通过。
- Rosetta 环境返回 `x86_64` 且 `sysctl.proc_translated = 1`，Intel slice 实际启动通过。
- Xcode Analyze 无诊断退出；当时名为 `verify-release.sh` 的完整验证通过（现行入口已重命名为 `verify-release-app.sh`）。

## 后台功耗实测与解释

同一台 Mac 上进行了 11 次、每 2 秒一次的空闲进程采样：

| 指标 | 优化前 Debug 基线 | 优化后签名 Release | 解读 |
|---|---:|---:|---|
| 平均 CPU（22 秒） | 0.182% | 0.218% | 都接近 0；短窗口差异不具有统计意义 |
| 刷新瞬间峰值 | 2.0% | 2.2% | 对应十秒刷新，没有持续忙循环 |
| 稳定 RSS | 约 115 MB | 约 119 MB | 构建配置与启动状态不同，不能据此判定内存回归 |

本轮可确认的功耗收益是结构性的：定时器可被系统合并、进程任务降为 utility、失效服务不再留下空唤醒，最大写盘频率降低约 81.3%。短时 CPU 采样只证明没有新增忙循环，不将噪声包装成“CPU 已下降”的结论。

## 新增回归保护

- 无新续航样本不写盘；首次样本、五分钟边界、暂停和退出冲刷均有测试。
- NaN、Infinity、负 CPU 和超过全部逻辑核心的 CPU 均有测试。
- 十种语言的新键存在性、日语动态字段、数字 locale、百分号和格式参数均有测试。
- 外部覆盖包按键合并、坏格式符回落、超过 2 MiB 的语言包拒绝均有测试。
- 发布验证新增 Swift 字面本地化键扫描和十种语言格式符签名对比。

## 产物与复现

- 最终签名 App：`.build/verify-release/DerivedData/Build/Products/Release/BatteryMonitor.app`
- arm64 空闲采样：`.build/qa/release-arm64-idle.tsv`
- arm64 运行日志：`.build/qa/release-arm64-run.log`
- x86_64 运行日志：`.build/qa/release-x86_64-run.log`
- 历史完整验证：`./Scripts/verify-release.sh`（现为 `./Scripts/verify-release-app.sh`）
- 静态分析：使用 `.build/qa/StaticAnalysisDerivedData` 执行 Release `xcodebuild analyze`

> 上述 `.build/qa/` 是本报告生成时的历史路径，不是现行测试入口。当前所有测试 App、二进制、日志和 DerivedData 必须位于固定可见的 `QATests/`，不得照此历史路径新建或执行测试产物。

在本报告对应的历史运行中，测试产物位于当时约定的项目 `.build` 目录，没有从系统随机临时目录执行，也没有生成或上传 App Store Connect 归档。该 `.build` 约定现已废止，不能用于新的测试。
